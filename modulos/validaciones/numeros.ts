
export const Pi = 3.14;

// hacer q este sea el modulo por defecto
//  export default(poner esta funcion por defecto de este archivo) function restar(a:number, b: number): number {
//     return a -b;
// }

 export default function restar(a:number, b: number): number {
    return a -b;
}
//  export function restar(a:number, b: number): number {
//     return a -b;
// }


